Drop your photo files here. Then in index.html, replace each `.gallery-placeholder` div with:

    <img src="assets/images/your-photo.jpg" alt="Description" />

The current site uses beautiful gradient placeholders so it looks complete out of the box.